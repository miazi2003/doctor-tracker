# Doctor Tracker Client

Next.js App Router client for Doctor Tracker.

## Development

Copy `.env.example` to `.env.local`, then run:

```bash
npm install
npm run dev
```

The development server is available at `http://localhost:3000` by default.
